# Environment
NODE_ENV="development"  # development | production

GEMINI_API_KEY=XXXX

# Database
DATABASE_URL="postgresql://ppto:password@localhost:5432/ppto"

# Google OAuth 2.0 Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
GOOGLE_REDIRECT_URI="http://localhost:3001/auth/google/callback"  # Backend URL + callback path

# Session Secret (min 32 caracteres para producción)
SESSION_SECRET="ppto-app-secret-change-in-production-min-32-chars"

# Frontend URL (para redirects después de OAuth)
# Development: http://localhost:5173
# Production: https://your-domain.com
FRONTEND_URL="http://localhost:5173"

# API Port (opcional, por defecto 3001)
API_PORT="3001"

# RPA API Key (para autenticación del robot de automatización)
# Generar un token seguro aleatorio de al menos 32 caracteres
# Ejemplo: openssl rand -base64 32
RPA_API_KEY="your-secure-random-token-change-in-production"

# ============ GOOGLE DRIVE (Documentos OC) ============
# Habilitar integración con Google Drive para documentos
GDRIVE_ENABLED="false"

# Carpeta raíz para cotizaciones PENDIENTES (donde se suben al crear OC)
# Obtener de la URL de la carpeta: https://drive.google.com/drive/folders/{FOLDER_ID}
GDRIVE_OC_ROOT_FOLDER_ID=""

# Carpeta raíz para INCIDENTES PROCESADOS (donde se mueven al generar incidente)
# Esta es una carpeta DIFERENTE, en otra ubicación de Drive
# Si no se configura, usará GDRIVE_OC_ROOT_FOLDER_ID como fallback
GDRIVE_INCIDENTS_FOLDER_ID=""

# Service Account JSON (codificado en base64 o ruta a archivo)
# Opción 1: Ruta a archivo JSON de credenciales
GDRIVE_SERVICE_ACCOUNT_PATH=""
# Opción 2: JSON completo codificado en base64 (para entornos sin filesystem)
# GDRIVE_SERVICE_ACCOUNT_BASE64=""

# Tamaño máximo de archivo en bytes (default: 10MB)
GDRIVE_MAX_FILE_SIZE="10485760"

# Tipos MIME permitidos (separados por coma)
GDRIVE_ALLOWED_MIME_TYPES="application/pdf"

# ============ N8N WEBHOOK (OC Procesada) ============
# URL del webhook de N8N para notificar cuando una OC se procesa
N8N_OC_PROCESADA_WEBHOOK_URL="http://10-43-5-105.nip.io:5678/webhook/b5168d4f-9fc4-4745-a4a8-05afabbbcce0"
