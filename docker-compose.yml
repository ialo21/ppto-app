version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ppto
      POSTGRES_PASSWORD: ppto
      POSTGRES_DB: ppto
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
